// iOS secrets — copy this file to `Secrets.xcconfig` (same folder) and fill in
// real values. The real Secrets.xcconfig is git-ignored.
//
// MAPS_API_KEY is substituted into ios/Runner/Info.plist via $(MAPS_API_KEY).
MAPS_API_KEY = YOUR_IOS_MAPS_API_KEY
